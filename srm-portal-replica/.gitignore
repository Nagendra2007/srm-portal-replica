node_modules/
.env
.env.*
!.env.example

# OCR language model — large binary, fetch it during deploy rather than
# committing it. Remove this line if you decide to vendor it instead.
*.traineddata

npm-debug.log*
.DS_Store
